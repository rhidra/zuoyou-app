import { MediaUploadDirective } from './media-upload.directive';

describe('MediaUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaUploadDirective();
    expect(directive).toBeTruthy();
  });
});
